{% extends 'base.html.twig' %}

{% block title %}Animals{% endblock %}

{% block body %}
    <h1>Animals</h1>

    



    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animal.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ animal.name }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ animal.size }}</td>
            </tr>
            <tr>
                <th>Lifetime</th>
                <td>{{ animal.lifetime }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ animal.phone }}</td>
            </tr>
            <tr>
                <th>MartialArt</th>
                <td>{{ animal.martialArt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_animals_index') }}">back to list</a>

    <a href="{{ path('app_animals_edit', {'id': animal.id}) }}">edit</a>

    {{ include('animals/_delete_form.html.twig') }}
{% endblock %}
